spring:
  application:
    name: eureka
  cloud:
    config:
      enabled: false
      security.basic.enable: false   

management:
  security:
    enabled: false 

zuul:
  host:
    socket-timeout-millis: 30000