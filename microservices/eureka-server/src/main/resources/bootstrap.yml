spring:
  application:
    name: eureka
  cloud:
    config:
      enabled: false
      security.basic.enable: false
management:
  security:
    enabled: false
  endpoints:
    web.exposure.include: "*"
  endpoint:
    health:
      show-details: ALWAYS
zuul:
  host:
    socket-timeout-millis: 30000